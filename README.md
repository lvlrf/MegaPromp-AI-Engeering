# راهنمای اجرایی فرآیند (V2) — جریان Chat-First → MVP Lock → Tech/Ops/API Lock → BUILD

این راهنما برای استفاده در ریپوی GitHub شما نوشته شده و با مگاپرامت `MEGAPROMPT_GEM_V6.5` هم‌راستا است. هدف: کاهش دوباره‌کاری، جلوگیری از لوپ‌های معیوب اصلاحات، و امکان جابجایی بین Agentها/مدل‌ها بدون کج‌فهمی.

---

## 1) فایل‌های ورودی/مرجع در روت پروژه

حداقل این دو فایل را در روت ریپو نگه دارید:
- `MEGAPROMPT_GEM_V6_5.md`  (مگاپرامت اصلی برای همفکری و Build)
- `PROCESS_EXECUTION_GUIDE_V2.md` (همین راهنما)

اختیاری (اگر قبلاً دارید، نگه دارید):
- فایل‌های مقایسه ابزارها/راهنمای OpenAPI/… (صرفاً آموزشی)

---

## 2) چرخه کار (چهار فاز اصلی)

### فاز A — DISCOVERY (همفکری و شفاف‌سازی)
**ورودی:** پیام‌های پراکنده شما (طوفان فکری)  
**خروجی:** نقش‌ها/یوزکیس‌ها/دیتامدل پیشنهادی + سوالات تکمیلی + پیشنهادات

روش اجرا:
1) یک چت جدید با مدل/Agent دلخواه باز کنید.
2) محتوای `MEGAPROMPT_GEM_V6_5.md` را در ابتدای گفتگو قرار دهید.
3) شروع کنید به توضیح دادن ایده/نیازها (نامرتب هم باشد مشکلی نیست).
4) Agent باید:
   - تناقض‌ها را همان لحظه از شما بپرسد و حل کند (حدس نزند)
   - نقش‌ها/Use Caseها/Entities را استخراج کند
   - پیشنهادهای محصولی/UX/فنی سبک بدهد (بدون قفل کردن معماری)

**نکته کلیدی:** در این مرحله محدودیت توکن نداریم. هدف شفافیت است.

---

### فاز B — FEATURE INVENTORY + MVP LOCK
**ورودی:** خروجی Discovery + توضیحات تکمیلی شما  
**خروجی:** چک‌لیست فیچرها با نشانگرها

Agent باید یک چک‌لیست گروه‌بندی‌شده تولید کند با این نشانگرها:
- `●` = در MVP (حتماً همین نسخه اول)
- `○` = بعداً (Post-MVP)
- `?` = نیازمند تصمیم
- `🚫` = خارج از محدوده (Out of Scope)

قانون مهم:
- ابتدا همه آیتم‌ها `○` هستند.
- Agent پیشنهاد می‌دهد کدام‌ها `●` شوند.
- شما ویرایش می‌کنید تا MVP قفل شود.

پس از تایید شما، Agent یک خلاصه می‌دهد:
- MVP LOCK SUMMARY (آیتم‌های ●)
- POST-MVP MEMORY (آیتم‌های ○)
- OUT-OF-SCOPE MEMORY (آیتم‌های 🚫)

---

### فاز C — TECH + OPS + API LOCK
**ورودی:** MVP قفل‌شده  
**خروجی:** تصمیم‌های فنی/زیرساختی/پایداری/دیباگ + تصمیم درباره چرخه OpenAPI

Agent باید برای هر بخش ۲–۳ گزینه با مزایا/معایب بدهد و سپس پیشنهاد بدهد:
- معماری: modular monolith / microservices / hybrid
- دیتابیس: انتخاب اصلی + جایگزین‌ها + منطق رشد
- استقرار: MVP ساده vs مسیر ارتقا (Scale/HA)
- Observability: لاگ ساخت‌یافته، correlation id، error shape، metrics، runbook
- امنیت و نگهداری: secrets، backup/restore، rate limit (در صورت نیاز)

#### تصمیم پویا: آیا `OPENSPEC_WORKFLOW.md` لازم است؟
در این فاز Agent باید:
1) از شما بپرسد API ساده است یا چند endpoint و تغییرپذیر؟
2) پیشنهاد بدهد:
   - یا فقط یک بخش کوتاه «API Contract Workflow» داخل `RULES.md`
   - یا افزودن فایل `docs/40_api/OPENSPEC_WORKFLOW.md`
3) نتیجه را اعلام کند و اجازه override بدهد.

پس از تایید شما، Agent یک «TECH & OPS & API LOCK CHECKLIST» می‌دهد و قفل می‌شود.

---

### فاز D — BUILD (ساخت فایل‌ها)
**فقط وقتی شما گفتید:** `BUILD` / `بساز` / `خروجی بده`

خروجی‌ها:
- **فایل‌های اجرایی/فنی فقط برای MVP** تولید می‌شوند:
  - `SPEC.md`, `TASKS.md`, `ARCHITECTURE.md`, `RULES.md`, `UIUX.md`, `OPENAPI.yaml`
- **هر چیزی که MVP نیست** فقط به انتهای `PRD.md` اضافه می‌شود:
  - Later Scope (Post-MVP)
  - Out of Scope

**نکته مهم:** `OPENAPI.yaml` را خود Agent می‌نویسد (فقط MVP).

---

## 3) ساختار فایل‌های خروجی (داخل repo)

این ساختار در BUILD تولید می‌شود:

```
docs/
  00_context/
    PRD.md
    PRD_NOTES.md
    GLOSSARY.md
    DECISIONS.md
  10_product/
    SPEC.md
    TASKS.md
  20_engineering/
    ARCHITECTURE.md
    RULES.md
  30_design/
    UIUX.md
  40_api/
    OPENAPI.yaml
    (OPENSPEC_WORKFLOW.md)  # فقط اگر تصمیم گرفته شود
  90_ops/
    CHANGELOG.md

# روت:
CLAUDE.md
CODEX.md
GEMINI.md
.cursorrules
.github/copilot-instructions.md
```

---

## 4) جلوگیری از لوپ اصلاحات (Anti-loop) — قانون طلایی

Agent باید در `TASKS.md` برای هر Task موارد زیر را الزام کند:
- **Files to Touch**: فایل‌های مجاز برای تغییر (حداقلی)
- **Acceptance Criteria**: چه چیزی باید درست شود
- **Verification Steps**: چه دستور/تستی اجرا شود
- **Definition of Done (DoD)**: ثبت نتیجه در CHANGELOG، و اگر DB تغییر کرد، migration/rollback

این باعث می‌شود:
- refactor بی‌پایان رخ ندهد
- اگر Agent عوض شد، نفر/مدل جدید بفهمد دقیقاً چه باید بکند

---

## 5) استفاده از OpenSpec/OpenAPI در عمل (حداقلی و کاربردی)

حداقل روال پیشنهادی:
1) هر تغییر API → اول `docs/40_api/OPENAPI.yaml` را آپدیت کن (spec-first).
2) سپس کد را مطابق اسپک پیاده‌سازی کن.
3) سپس verify:
   - validate spec (هر ابزار موجود)
   - smoke test endpoint
4) ثبت در `CHANGELOG.md`:
   - کدام endpoint/schema تغییر کرد
   - چه تست‌هایی اجرا شد

اگر `OPENSPEC_WORKFLOW.md` ساخته شد، همان فایل روال import/export و validate را دقیق‌تر شرح می‌دهد.

---

## 6) پیشنهاد برای مدیریت نسخه‌ها و انتقال بین مدل‌ها

برای اینکه هر زمان خواستید مدل/Agent را عوض کنید، گیر نکنید:
- همیشه از `DECISIONS.md` شروع کنید (تصمیم‌های قفل‌شده)
- سپس `TASKS.md` (قدم بعدی چیست)
- سپس `CHANGELOG.md` (آخرین وضعیت اجرا)

این سه فایل، “state” پروژه هستند.

---

## 7) Checklist سریع قبل از BUILD
قبل از اینکه بگویید BUILD، مطمئن شوید:
- MVP قفل شده (لیست ● نهایی است)
- Tech/Ops/API lock تایید شده
- اگر API دارید، error shape و auth/permissions حداقل مشخص شده
- درباره نیاز/عدم نیاز `OPENSPEC_WORKFLOW.md` تصمیم گرفته‌اید (یا اجازه داده‌اید Agent پیشنهاد بدهد)

---

## 8) فرمان‌های پیشنهادی برای شما در چت
- «Phase 1 ادامه بده، سوالاتت رو بپرس و خروجی roles/usecases/entities بده.»
- «Phase 2 رو بساز: چک‌لیست کامل فیچرها با ●/○/?/🚫.»
- «این‌ها رو ● کن، این‌ها ○ بمونه، این یکی 🚫.»
- «Phase 3: گزینه‌های معماری/DB/infra/monitoring رو با هزینه/زمان/ریسک بگو.»
- «BUILD» (وقتی همه قفل شد)

---
