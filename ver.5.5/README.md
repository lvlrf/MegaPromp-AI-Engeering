# راهنمای اجرایی فرآیند (V2) — جریان Chat-First → MVP Lock → Tech/Ops/API Lock → BUILD

این راهنما برای استفاده در ریپوی GitHub شما نوشته شده و با مگاپرامت `MEGAPROMPT_GEM_V6.5` هم‌راستا است. هدف: کاهش دوباره‌کاری، جلوگیری از لوپ‌های معیوب اصلاحات، و امکان جابجایی بین Agentها/مدل‌ها بدون کج‌فهمی.

---

## 1) فایل‌های ورودی/مرجع در روت پروژه

حداقل این دو فایل را در روت ریپو نگه دارید:
- `MEGAPROMPT_GEM_V6_5.md`  (مگاپرامت اصلی برای همفکری و Build)
- `PROCESS_EXECUTION_GUIDE_V2.md` (همین راهنما)

اختیاری (اگر قبلاً دارید، نگه دارید):
- فایل‌های مقایسه ابزارها/راهنمای OpenAPI/… (صرفاً آموزشی)

---

## 2) چرخه کار (چهار فاز اصلی)

### فاز A — DISCOVERY (همفکری و شفاف‌سازی)
**ورودی:** پیام‌های پراکنده شما (طوفان فکری)  
**خروجی:** نقش‌ها/یوزکیس‌ها/دیتامدل پیشنهادی + سوالات تکمیلی + پیشنهادات

روش اجرا:
1) یک چت جدید با مدل/Agent دلخواه باز کنید.
2) محتوای `MEGAPROMPT_GEM_V6_5.md` را در ابتدای گفتگو قرار دهید.
3) شروع کنید به توضیح دادن ایده/نیازها (نامرتب هم باشد مشکلی نیست).
4) Agent باید:
   - تناقض‌ها را همان لحظه از شما بپرسد و حل کند (حدس نزند)
   - نقش‌ها/Use Caseها/Entities را استخراج کند
   - پیشنهادهای محصولی/UX/فنی سبک بدهد (بدون قفل کردن معماری)

**نکته کلیدی:** در این مرحله محدودیت توکن نداریم. هدف شفافیت است.

---

### فاز B — FEATURE INVENTORY + MVP LOCK
**ورودی:** خروجی Discovery + توضیحات تکمیلی شما  
**خروجی:** چک‌لیست فیچرها با نشانگرها

Agent باید یک چک‌لیست گروه‌بندی‌شده تولید کند با این نشانگرها:
- `●` = در MVP (حتماً همین نسخه اول)
- `○` = بعداً (Post-MVP)
- `?` = نیازمند تصمیم
- `🚫` = خارج از محدوده (Out of Scope)

قانون مهم:
- ابتدا همه آیتم‌ها `○` هستند.
- Agent پیشنهاد می‌دهد کدام‌ها `●` شوند.
- شما ویرایش می‌کنید تا MVP قفل شود.

پس از تایید شما، Agent یک خلاصه می‌دهد:
- MVP LOCK SUMMARY (آیتم‌های ●)
- POST-MVP MEMORY (آیتم‌های ○)
- OUT-OF-SCOPE MEMORY (آیتم‌های 🚫)

---

### فاز C — TECH + OPS + API LOCK
**ورودی:** MVP قفل‌شده  
**خروجی:** تصمیم‌های فنی/زیرساختی/پایداری/دیباگ + تصمیم درباره چرخه OpenAPI

Agent باید برای هر بخش ۲–۳ گزینه با مزایا/معایب بدهد و سپس پیشنهاد بدهد:
- معماری: modular monolith / microservices / hybrid
- دیتابیس: انتخاب اصلی + جایگزین‌ها + منطق رشد
- استقرار: MVP ساده vs مسیر ارتقا (Scale/HA)
- Observability: لاگ ساخت‌یافته، correlation id، error shape، metrics، runbook
- امنیت و نگهداری: secrets، backup/restore، rate limit (در صورت نیاز)

#### تصمیم پویا: آیا `OPENSPEC_WORKFLOW.md` لازم است؟
در این فاز Agent باید:
1) از شما بپرسد API ساده است یا چند endpoint و تغییرپذیر؟
2) پیشنهاد بدهد:
   - یا فقط یک بخش کوتاه «API Contract Workflow» داخل `RULES.md`
   - یا افزودن فایل `docs/40_api/OPENSPEC_WORKFLOW.md`
3) نتیجه را اعلام کند و اجازه override بدهد.

پس از تایید شما، Agent یک «TECH & OPS & API LOCK CHECKLIST» می‌دهد و قفل می‌شود.

---

### فاز D — BUILD (ساخت فایل‌ها)
**فقط وقتی شما گفتید:** `BUILD` / `بساز` / `خروجی بده`

خروجی‌ها:
- **فایل‌های اجرایی/فنی فقط برای MVP** تولید می‌شوند:
  - `SPEC.md`, `TASKS.md`, `ARCHITECTURE.md`, `RULES.md`, `UIUX.md`, `OPENAPI.yaml`
- **هر چیزی که MVP نیست** فقط به انتهای `PRD.md` اضافه می‌شود:
  - Later Scope (Post-MVP)
  - Out of Scope

**نکته مهم:** `OPENAPI.yaml` را خود Agent می‌نویسد (فقط MVP).

---

## 3) ساختار فایل‌های خروجی (داخل repo)

این ساختار در BUILD تولید می‌شود:

```
docs/
  00_context/
    PRD.md
    PRD_NOTES.md
    GLOSSARY.md
    DECISIONS.md
  10_product/
    SPEC.md
    TASKS.md
  20_engineering/
    ARCHITECTURE.md
    RULES.md
  30_design/
    UIUX.md
  40_api/
    OPENAPI.yaml
    (OPENSPEC_WORKFLOW.md)  # فقط اگر تصمیم گرفته شود
  90_ops/
    CHANGELOG.md

# روت:
CLAUDE.md
CODEX.md
GEMINI.md
.cursorrules
.github/copilot-instructions.md
```

---

## 4) جلوگیری از لوپ اصلاحات (Anti-loop) — قانون طلایی

Agent باید در `TASKS.md` برای هر Task موارد زیر را الزام کند:
- **Files to Touch**: فایل‌های مجاز برای تغییر (حداقلی)
- **Acceptance Criteria**: چه چیزی باید درست شود
- **Verification Steps**: چه دستور/تستی اجرا شود
- **Definition of Done (DoD)**: ثبت نتیجه در CHANGELOG، و اگر DB تغییر کرد، migration/rollback

این باعث می‌شود:
- refactor بی‌پایان رخ ندهد
- اگر Agent عوض شد، نفر/مدل جدید بفهمد دقیقاً چه باید بکند

---

## 5) استفاده از OpenSpec/OpenAPI در عمل (حداقلی و کاربردی)

حداقل روال پیشنهادی:
1) هر تغییر API → اول `docs/40_api/OPENAPI.yaml` را آپدیت کن (spec-first).
2) سپس کد را مطابق اسپک پیاده‌سازی کن.
3) سپس verify:
   - validate spec (هر ابزار موجود)
   - smoke test endpoint
4) ثبت در `CHANGELOG.md`:
   - کدام endpoint/schema تغییر کرد
   - چه تست‌هایی اجرا شد

اگر `OPENSPEC_WORKFLOW.md` ساخته شد، همان فایل روال import/export و validate را دقیق‌تر شرح می‌دهد.

---

## 6) پیشنهاد برای مدیریت نسخه‌ها و انتقال بین مدل‌ها

برای اینکه هر زمان خواستید مدل/Agent را عوض کنید، گیر نکنید:
- همیشه از `DECISIONS.md` شروع کنید (تصمیم‌های قفل‌شده)
- سپس `TASKS.md` (قدم بعدی چیست)
- سپس `CHANGELOG.md` (آخرین وضعیت اجرا)

این سه فایل، “state” پروژه هستند.

---

## 7) Checklist سریع قبل از BUILD
قبل از اینکه بگویید BUILD، مطمئن شوید:
- MVP قفل شده (لیست ● نهایی است)
- Tech/Ops/API lock تایید شده
- اگر API دارید، error shape و auth/permissions حداقل مشخص شده
- درباره نیاز/عدم نیاز `OPENSPEC_WORKFLOW.md` تصمیم گرفته‌اید (یا اجازه داده‌اید Agent پیشنهاد بدهد)

---

## 8) فرمان‌های پیشنهادی برای شما در چت
- «Phase 1 ادامه بده، سوالاتت رو بپرس و خروجی roles/usecases/entities بده.»
- «Phase 2 رو بساز: چک‌لیست کامل فیچرها با ●/○/?/🚫.»
- «این‌ها رو ● کن، این‌ها ○ بمونه، این یکی 🚫.»
- «Phase 3: گزینه‌های معماری/DB/infra/monitoring رو با هزینه/زمان/ریسک بگو.»
- «BUILD» (وقتی همه قفل شد)

---



# راهنمای اجرایی کامل Vibe Coding (v6.5)

این فایل را در روت ریپازیتوری قرار بده (مثلاً `docs/PROCESS_EXECUTION_GUIDE.md`) تا یک دستورالعمل قدم‌به‌قدم، قابل‌اتکا و قابل‌تکرار برای اجرای پروژه با AI coding agents داشته باشی؛ از مرحله‌ی ایده و MVP تا OpenAPI، Mock، SDK، تست قرارداد (Contract) و اجرای تسک‌ها—بدون افتادن در لوپ اصلاح و دوباره‌کاری.

---

## 0) پیش‌نیازها

### ابزارهای عمومی
- Git
- Node.js >= 20 (برای OpenSpec و برخی ابزارهای OpenAPI/Mock)
- Docker Desktop (اختیاری ولی توصیه‌شده: برای Swagger UI/Editor، Prism، OpenAPI Generator بدون درگیری نصب)
- زبان/استک پروژه (Python/Go/Node/…) — بعد از تایید MVP تصمیم می‌گیریم.

### فایل‌هایی که باید در روت پروژه باشد
(این‌ها همان فایل‌هایی هستند که از پَک v6.5 در روت می‌ریزی)
- `EXECUTOR_CORE.md`  (یا `EXECUTOR_CORE_V6_5.txt` را به این نام تغییر بده)
- فایل‌های راهنمای مخصوص ایجنت‌ها: `CODEX.md`, `CLAUDE.md`, `GEMINI.md`, `.github/copilot-instructions.md`, `.cursorrules`, `AGENT_HANDOFF_README.md`

> این فایل‌ها کد تولید نمی‌کنند؛ «قرارداد اجرا» هستند تا پروژه بین چند ایجنت/مدل قابل ادامه باشد و refactor بی‌پایان رخ ندهد.

---

## 1) مدل ذهنی فرآیند: دو فازِ جدا

### فاز A — هم‌فکری و شفاف‌سازی (Chat‑First)
خروجی این فاز «اسناد» است، نه کد:
- نیازها را مرتب می‌کنیم (بدون دست‌کاری محتوا؛ فقط نظم + سؤال برای رفع ابهام)
- چک‌لیست MVP را می‌سازیم و تو تأیید می‌کنی
- سپس تصمیم‌های فنی را با تحلیل trade‑off می‌بندیم

**خروجی‌ها (در مسیر `docs/`):**
- `docs/00_context/PRD.md`
- `docs/00_context/PRD_NOTES.md`  (فقط چیزهایی که نیاز به تصمیم/پرسش دارند)
- `docs/10_product/SPEC.md`
- `docs/10_product/TASKS.md`
- `docs/20_engineering/ARCHITECTURE.md`
- `docs/20_engineering/RULES.md`
- `docs/30_design/UIUX.md` (اگر درباره UI/UX صحبت شد)
- `docs/40_api/OPENAPI.yaml` (اگر پروژه/API نیاز داشته باشد)
- `docs/90_ops/CHANGELOG.md`

### فاز B — اجرا (Coding‑Agent Execution)
ایجنت کدنویسی فقط بعد از تایید MVP و تصمیمات فنی وارد «اعمال تغییرات» می‌شود:
- تسک‌ها را از `TASKS.md` اجرا می‌کند
- هر تسک **Definition of Done** دارد: verify، تست، ثبت در changelog، و اگر DB تغییر کرد migration + rollback
- خروجی باید handoff‑friendly باشد (اگر ایجنت/مدل عوض شد، ادامه‌پذیر بماند)

---

## 2) شروع سریع (مسیر پیشنهادی)

### قدم 1: فایل‌های پایه را در روت بریز + commit اولیه
```bash
git init
git add .
git commit -m "chore: add agent execution scaffolding (v6.5)"
```

### قدم 2: وارد فاز هم‌فکری شو (با MEGAPROMPT_GEM_V6.5)
- MEGAPROMPT را به عنوان context به چت‌بات بده.
- خروجی مورد انتظار:
  1) PRD مرتب‌شده از پیام‌های تو (بدون حدس زدن)
  2) چک‌لیست نیازها + MVP پیشنهادی (دایره‌های پر/خالی)
  3) بعد از تایید تو: TASKS و تصمیمات فنی و در صورت نیاز OpenAPI

### قدم 3: MVP را تأیید کن
- همه آیتم‌ها ابتدا ⭕
- آیتم‌هایی که چت‌بات برای MVP پیشنهاد می‌دهد: ●
- تو کم/زیاد می‌کنی، و نسخه نهایی MVP را می‌دهی.

### قدم 4: تصمیمات فنی را ببند
- انتخاب استک + DB + معماری (مونولیت ماژولار / میکروسرویس ترکیبی) + امنیت پایه + لاگ/مانیتورینگ + HA
- خروجی: `ARCHITECTURE.md` و `RULES.md` و در صورت نیاز `OPENAPI.yaml`

### قدم 5: اجرای ایجنت
- ایجنت را با دستورالعمل روشن اجرا کن: «اول EXECUTOR_CORE + فایل مخصوص خودت + docs/* را بخوان، بعد TASKS را فقط برای MVP اجرا کن.»

---

## 3) OpenSpec دقیقاً کجا کمک می‌کند؟

OpenSpec یک لایه spec‑driven است برای اینکه هر تغییر/فیچر به شکل «بسته تغییر» (proposal + tasks + spec delta) شفاف و قابل ردیابی باشد.

**زمان مناسب استفاده**
- پروژه متوسط/بزرگ یا چند آپدیت پشت‌سرهم
- وقتی احتمال تغییر ایجنت/مدل هست (handoff)
- وقتی scope creep و دوباره‌کاری زیاد می‌شود

**زمانی که می‌توان حذفش کرد**
- پروژه خیلی کوچک و یک‌باره
- تغییرات کم و بدون تیم/فازهای متعدد

> نکته مهم: در خود مگاپرامت هم باید اختیار داشته باشد که با توجه به اندازه پروژه پیشنهاد بده OpenSpec فعال شود یا نه، و تو تصمیم نهایی را بگیری.

---

## 4) نصب و راه‌اندازی OpenSpec (اگر نیاز شد)

### 4.1 نصب
```bash
npm install -g @fission-ai/openspec@latest
openspec --version
```

### 4.2 init در پروژه
```bash
cd /path/to/your/repo
openspec init
```

### 4.3 چرخه کار استاندارد (Proposal → Apply → Archive)
- Proposal: «Create an OpenSpec change proposal for …»
- Apply: «Apply/Implement this OpenSpec change»
- Archive: `openspec archive <change> --yes`

---

## 5) ابزارهای OpenAPI و اینکه کجا وارد فرآیند می‌شوند

این بخش فقط وقتی فعال است که پروژه API داشته باشد یا قرار باشد SDK/Docs/Mock داشته باشی.

### 5.1 Swagger Editor (طراحی/ویرایش OpenAPI)
- سریع‌ترین: نسخه وب (editor.swagger.io)
- اجرای Docker (برای محیط محلی):
```bash
docker run -d -p 80:80 -e PORT=80 docker.swagger.io/swaggerapi/swagger-editor
```

### 5.2 Swagger UI (Interactive Docs)
Docker Quick Run:
```bash
docker run -p 80:8080 -e SWAGGER_JSON=/foo/OPENAPI.yaml -v $(pwd)/docs/40_api:/foo docker.swagger.io/swaggerapi/swagger-ui
```

### 5.3 Prism (Mock Server + Validation Proxy)
نصب (Node):
```bash
npm install -g @stoplight/prism-cli
```

Mock با فایل:
```bash
prism mock docs/40_api/OPENAPI.yaml
```

Proxy برای اعتبارسنجی پیاده‌سازی در برابر Spec:
```bash
prism proxy docs/40_api/OPENAPI.yaml http://localhost:PORT_OF_REAL_API
```

Docker (اگر نمی‌خواهی Node نصب کنی — نمونه رایج):
```bash
docker run --init -p 4010:4010 stoplight/prism:4 mock -h 0.0.0.0 docs/40_api/OPENAPI.yaml
```

### 5.4 OpenAPI Generator (SDK / Stubs)
نصب:
```bash
npm install -g @openapitools/openapi-generator-cli
openapi-generator-cli version
```

تولید SDK نمونه:
```bash
openapi-generator-cli generate -i docs/40_api/OPENAPI.yaml -g python -o sdk/python
```

### 5.5 Dredd (Contract Testing)
- این ابزار برای «تست قرارداد» است: اجرای تست بر اساس OpenAPI و مقایسه رفتار API واقعی با Spec.
- توصیه اجرایی: وقتی API MVP راه افتاد، Dredd را به CI اضافه کن تا هر PR شکستن قرارداد را سریع نشان دهد.

---

## 6) نصب و راه‌اندازی ایجنت‌های کدنویسی (خلاصه عملی)

### 6.1 Claude Code
- macOS/Linux/WSL:
```bash
curl -fsSL https://claude.ai/install.sh | bash
claude
```
- Windows PowerShell:
```powershell
irm https://claude.ai/install.ps1 | iex
claude
```

### 6.2 Gemini CLI
```bash
npm install -g @google/gemini-cli
gemini
```

### 6.3 GitHub Copilot CLI (Windows)
```powershell
winget install GitHub.Copilot
```

### 6.4 Codex CLI (policy و sandbox)
- هدف تو: کم کردن سوال‌های تایید در حین اجرا.
- روش: با تنظیمات رسمی Codex CLI، approval policy و sandbox را مطابق نیازت تنظیم کن.
- توصیه: اول یک اجرای کوتاه با تنظیمات امن‌تر؛ بعد اگر مطمئن شدی، سخت‌گیری را کم کن.

---

## 7) CodeRabbit (Review‑Automation برای کاهش دوباره‌کاری)
اگر روی GitHub PR می‌زنی و می‌خواهی ریویو خودکار داشته باشی:
- فایل `.coderabbit.yaml` باید در **روت** ریپو باشد.
- می‌توانی یک کانفیگ سبک بسازی که:
  - از تغییرات خارج از scope جلوگیری کند
  - چک کند Task/DoD رعایت شده یا نه
  - در PR summary بده

---

## 8) Template Promptهای اجرایی (برای اجرا بدون Loop)

### 8.1 اجرای کامل MVP (تک‌Agent)
«این ریپو را بررسی کن. اول `EXECUTOR_CORE.md` و فایل راهنمای خودت (`CODEX.md` یا `CLAUDE.md` یا `GEMINI.md`) را بخوان. بعد این فایل‌ها را source of truth بدان و اجرا کن:  
`docs/00_context/PRD.md`, `docs/10_product/SPEC.md`, `docs/10_product/TASKS.md`, `docs/20_engineering/RULES.md`, `docs/90_ops/CHANGELOG.md`.  
فقط تسک‌های MVP را اجرا کن. هر تسک را با DoD کامل کن (verify/test/changelog/migration+rollback).  
در پایان فقط یکی از این وضعیت‌ها را بده: DONE یا BLOCKED + دلیل دقیق.»

### 8.2 اجرای یک تسک مشخص
«فقط `TASK-003` را اجرا کن. اگر پیش‌نیازها ناقص است، دقیقاً لیست کن چه چیزی کم است و چرا.»

### 8.3 ابزارهای API (فقط اگر API داریم)
«اگر API داریم: `docs/40_api/OPENAPI.yaml` را کامل کن و سپس Prism mock و Swagger UI را راه‌اندازی کن؛ اگر SDK لازم است با OpenAPI Generator تولید کن؛ همه خروجی‌ها باید در مسیرهای مشخص باشند و در CHANGELOG ثبت شوند.»

---

## 9) استانداردهای ضد‑لوپ و ضد‑دوباره‌کاری (Troubleshooting‑Friendly)
این‌ها را در `RULES.md` و DoD تسک‌ها enforce کن:

- Logging ساختارمند (حداقل: request_id / user_id / error_code)
- طبقه‌بندی خطا (Error taxonomy): کد خطا + پیام انسانی + پیام developer
- Runbook کوتاه برای خطاهای پرتکرار
- Health checks (readiness/liveness اگر سرویس‌محور شد)
- Observability حداقلی (metrics پایه + کنترل log level)
- دستور اجرای تست‌ها + smoke test

---

## 10) پاسخ به سؤال «مگاپرامت‌ها کوتاه هستند، کافی است؟»
بله—اگر طراحی‌شان “Index Prompt” باشد:
- یعنی به جای اینکه همه چیز داخل prompt باشد، prompt مشخص کند **کدام فایل‌ها منبع حقیقت‌اند** و **قواعد اجرا چیست**.
- در این صورت هم دقیق‌تر است هم ارزان‌تر.

اما برای فاز هم‌فکری (Chat‑First) کوتاه بودن شرط نیست و می‌توان مفصل نوشت (کاری که تو می‌خواهی) تا ابهام صفر شود و بعداً هزینه‌ی refactor ندهی.

---

## 11) چک نهایی قبل از اجرای ایجنت
- [ ] MVP تایید شده
- [ ] TASKS.md تسک‌های MVP واضح و قابل اجرا هستند
- [ ] RULES.md و DoD روشن است
- [ ] ARCHITECTURE.md تصمیمات کلیدی را دارد (DB، دیپلوی، امنیت پایه)
- [ ] اگر API داریم: OPENAPI.yaml حداقل endpointهای MVP را دارد


---

## 12) تنظیم Codex CLI برای اینکه کمتر از تو تایید بگیرد (مسیر رسمی)

> هدف: Codex بتواند داخل workspace به‌صورت خودکار فایل بخواند/تغییر دهد/کامند اجرا کند و فقط در موارد پرریسک (خارج از ورک‌دیرکتوری یا شبکه) از تو تایید بگیرد.  
> نکته: تنظیمات دقیق را با مستندات همان نسخه‌ای که نصب داری مچ کن.

### 12.1 محل فایل کانفیگ
Codex از فایل زیر می‌خواند:
- `~/.codex/config.toml`

### 12.2 الگوی تنظیمات رایج
نمونه تنظیمات (حداقل):
```toml
# %USERPROFILE%\.codex\config.toml

approval_policy = "never"
sandbox_mode = "danger-full-access"
model = "gpt-5.2-codex"
model_reasoning_effort = "medium"

[features]
web_search_request = true

```

### 12.3 پیشنهاد اجرایی برای جلوگیری از اتلاف وقت
- برای پروژه‌های حساس: شبکه را خاموش نگه دار و فقط وقتی لازم شد روشن کن.
- برای پروژه‌های معمول: شبکه را فقط برای dependency install یا دانلود ابزارها روشن کن، بعد خاموشش کن.
- همیشه داخل ریپوی git‌دار کار کن تا Codex هم «Auto» و هم فضای کار را درست تشخیص دهد.

### 12.4 دستور اجرا شروع کار CODEX
```
Hard rules:
- Do NOT add any new features/add-ons beyond what is explicitly specified in this repo docs.
- Do NOT do code changes in Phase 1.
- If you think anything extra is needed, write a short RFC in docs/PRD_NOTES.md and STOP.
- Only ask me a question if you are blocked by missing critical information. Otherwise make the safest assumption and proceed, and log it in docs/PRD_NOTES.md.

Phase 1 — Read & Understand (no code changes yet):
1) There is NO README. Instead read these first:
   - EXECUTOR_CORE.md
   - CODEX.md
   - AGENTS.md
   - AGENT_HANDOFF_README.md
2) Read docs/** and openspec/** completely.
3) Inspect the repository structure (list key folders/files).
4) Identify the current goal of this project and missing/blocked parts.
5) Identify how the project is supposed to be run (commands, env vars, ports, etc.).
6) If there is an OpenAPI spec, locate it and summarize endpoints/models at a high level.

Phase 2 — Plan:
Produce a step-by-step implementation plan with small tasks.
For each task, include:
- files to change
- commands to run to verify
- expected outcome
Do not start implementing until the plan is written.

Phase 3 — Execute:
Execute the plan task-by-task.
After each task:
- show what changed (file paths + brief summary)
- run verification commands
- report results

Also:
- Create/update docs/PRD_NOTES.md and record: repo snapshot, extracted requirements checklist, assumptions, plan, and progress log.
```
---

## 13) گیت و ورژن کنترل برای جلوگیری از آشفتگی

### 13.1 شاخه‌ها
پیشنهاد حداقلی:
- `main`: پایدار
- `dev`: ادغام تغییرات
- `feature/<short-name>`: هر تسک/فیچر

### 13.2 سبک کامیت
اگر می‌خواهی future‑proof باشد:
- `feat: ...`
- `fix: ...`
- `chore: ...`
- `docs: ...`
- `refactor: ...` (فقط وقتی واقعاً ضروری است و در scope تسک آمده)

### 13.3 تگ نسخه‌ای
در پایان هر milestone MVP:
```bash
git tag -a v0.1.0 -m "MVP baseline"
git push --tags
```

---

## 14) چرا docs/40_api/OPENSPEC_WORKFLOW.md را نگه داریم؟

این فایل دو کاربرد دارد:
1) **برای انسان**: وقتی بعداً برگشتی یا یک نفر دیگر وارد پروژه شد، سریع بفهمد OpenSpec چه می‌کند و چگونه فیچرها را به proposal/task تبدیل می‌کنیم.
2) **برای ایجنت‌ها**: بعضی ایجنت‌ها/CLIها slash command یا flow خاص دارند. این فایل «توضیح درجا» است تا ایجنت‌ها بدون حدس زدن کار کنند.

اگر پروژه خیلی کوچک شد و OpenSpec را حذف کردی، این فایل هم می‌تواند حذف شود.

