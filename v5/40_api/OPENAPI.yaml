openapi: 3.0.3
info:
  title: Project API
  version: 0.1.0
servers:
  - url: http://localhost:8000
paths: {}
components:
  schemas:
    ErrorResponse:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
